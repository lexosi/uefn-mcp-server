[pytest]
pythonpath = .
testpaths = tests
addopts = -ra --strict-markers
markers =
    live: requires a running UEFN listener on ports 8765-8770 (auto-skips if unreachable)
    canary: negative-control test that must FAIL if the code regresses or the harness is broken
